<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.carddesign.mapper.PushTimeMapper">

    <insert id="insertPushTime" parameterType="com.example.carddesign.vo.PushTime" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO push_time (
        push_config_id, weekday, month_day, hour, enable_flag, create_user, create_time, update_user, update_time
        ) VALUES (
        #{pushConfigId}, #{weekday}, #{monthDay}, #{hour}, #{enableFlag}, #{createUser}, #{createTime}, #{updateUser}, #{updateTime}
        )
    </insert>

    <select id="selectPushTimesByConfigId" parameterType="java.lang.Long" resultType="com.example.carddesign.vo.PushTime">
        SELECT
        id, push_config_id, weekday, month_day, hour, enable_flag, create_user, create_time, update_user, update_time
        FROM
        push_time
        WHERE
        push_config_id = #{pushConfigId} AND enable_flag = 1
    </select>

    <update id="updatePushTime" parameterType="com.example.carddesign.vo.PushTime">
        UPDATE push_time
        <set>
            <if test="weekday != null">
                weekday = #{weekday},
            </if>
            <if test="monthDay != null">
                month_day = #{monthDay},
            </if>
            <if test="hour != null">
                hour = #{hour},
            </if>
            <if test="enableFlag != null">
                enable_flag = #{enableFlag},
            </if>
            update_user = #{updateUser},
            update_time = #{updateTime}
        </set>
        WHERE id = #{id}
    </update>

    <update id="logicDeletePushTime" parameterType="java.lang.Long">
        UPDATE push_time
        SET
        enable_flag = 0,
        update_user = -1,
        update_time = NOW()
        WHERE
        id = #{id}
    </update>

    <update id="logicDeletePushTimesByConfigId" parameterType="java.lang.Long">
        UPDATE push_time
        SET
        enable_flag = 0,
        update_user = -1,
        update_time = NOW()
        WHERE
        push_config_id = #{pushConfigId}
    </update>

    <delete id="deletePushTimesByConfigId" parameterType="java.lang.Long">
        DELETE FROM push_time
        WHERE push_config_id = #{pushConfigId}
    </delete>

</mapper>